
SELECT * FROM Proposal WHERE from_time > 'September 21 13:00:00 2015 EST' AND until_time <  'September 23 22:00:00 2015 EST' ;

SELECT Area.text, Restaurant.name
FROM Area
INNER JOIN Restaurant
ON Area.zip=Restaurant.zip;

SELECT SUM(ideal_size) 
FROM Proposal 
WHERE ideal_size > 2;